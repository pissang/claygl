// @ts-nocheck
import Base from '../core/Base';

const CanvasMaterial = Base.extend({
  color: [1, 1, 1, 1],

  opacity: 1,

  pointSize: 0,

  pointShape: 'rectangle'
});

export default CanvasMaterial;
